name: Publish package to the Maven Central Repository

on:
  release:
    types: [created]

jobs:
  publish:
    uses: reallifedeveloper/github-workflows/.github/workflows/maven-deploy.yaml@v20250704.2
    permissions:
      contents: read
    secrets: inherit
